import { siteTitle } from "../components/layout";

export default function contact() {
    return (
        <Layout>
            <Head>
                <title>{siteTitle}</title>
            </Head>
        </Layout>
    );
};